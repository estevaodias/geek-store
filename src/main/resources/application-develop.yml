spring:
  datasource:
    password: ${GEEKSTORE_DATABASE_PASSWORD}
    url: ${GEEKSTORE_DATABASE_URL}
    username: ${GEEKSTORE_DATABASE_USERNAME}
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true
